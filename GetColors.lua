-- ============================================================
-- PLUGIN BLOCK COLOR IN SCHEMATIC (required)
-- Returns { R, G, B } or { R, G, B, Alpha } -- range 0-255
-- ============================================================
function GetColor(props)
  return Colors.Plugin
end
